<% if @band.errors.any? %>
  <div id="error_explanation">
    <div class="alert alert-danger">
       <%= pluralize(@band.errors.count, "エラーが発生しました") %>.
    </div>
    <ul>
      <% @band.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
    </ul>
  </div>
<% end %>