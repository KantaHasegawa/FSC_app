<h1>メンバーを招待する</h1>
<div class="col-md-6 col-md-offset-3">
  <%= react_component("Invitation",token: form_authenticity_token, band_id: params[:id], collections: @collections ) %>
  <%= link_to 'キャンセル', @band, class: 'btn btn-info' %>
</div>
