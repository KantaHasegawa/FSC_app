<% @collections = User.pluck(:name, :id).unshift(['募集中', 0]) %>
<div class="col label-margin">
  <div class="account-wall">
    <div class="chase-logo">
      <h1 class="text-center login-title top-interval">バンドを作成する</h1>
      <p class="text-center normal-text ">使いやすいようにバンド名は正式名称で入力してください</p>
      <%= render "shared/error_messages" %>
    </div>
  <%= react_component("BandCreate",token: form_authenticity_token, current_user_id: current_user.id.to_s, collections: @collections) %>
  <%= link_to 'キャンセル', @band, class: 'btn btn-info btn-block' %>
</div>
</div>
</div>
</div>
